# BrighterTools.Payments.Stripe

Reusable Stripe integration service and models.

## Registration

```csharp
services.AddBrighterToolsStripePayments(configuration.GetSection(StripePaymentsOptions.SectionName));
```
